{
  "name": "pbl-obsidian-sync-client",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "build": "npm run clean && esbuild main.ts --bundle --platform=node --external:obsidian --outfile=main.js",
    "clean": "rimraf main.js main.js.map"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "esbuild": "^0.20.2",
    "obsidian": "^1.4.16",
    "rimraf": "^5.0.5",
    "typescript": "^5.9.3"
  }
}
